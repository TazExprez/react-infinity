import React, { Component } from "react";

const Spend = () => {
    return (
        <section id="Spend">
            <div className="container">
                Spend
            </div>
        </section>
    );
};

export default Spend;