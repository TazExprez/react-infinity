import React, { Component } from "react";

const Save = () => {
    return (
        <section id="Save">
            <div className="container">
                Save
            </div>
        </section>
    );
};

export default Save;